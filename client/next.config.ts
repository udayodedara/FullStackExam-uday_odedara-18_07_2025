import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['localhost', 'fullstackexam-uday-odedara-18-07-2025.onrender.com'],
  },
};

export default nextConfig;
